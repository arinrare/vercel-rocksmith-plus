{
  "name": "vercel-rocksmith-plus",
  "version": "1.0.0",
  "description": "A web interfasce for retrieving Rocksmith+ song recommendations",
  "main": "index.js",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel deploy --prod",
    "flask": "python src/app.py",
    "dev": "venv\\Scripts\\activate && set FLASK_APP=src/app.py && python -m flask run --debug",
    "build": "pip install -r requirements.txt"
  },
  "author": "Michael Baggott",
  "license": "MIT",
  "devDependencies": {
    "vercel": "^41.6.1"
  }
}
