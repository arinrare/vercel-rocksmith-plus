{
    "name": "rocksmith-plus-recommendations",
    "scripts": {
      "setup": "python -m venv venv && .\\venv\\Scripts\\activate && pip install -r requirements.txt",
      "dev": ".\\venv\\Scripts\\activate && set FLASK_APP=src/app.py && set FLASK_ENV=development && set FLASK_DEBUG=1 && python -m flask run",
      "clean": "deactivate 2>nul & rmdir /s /q venv 2>nul"
    }
  }